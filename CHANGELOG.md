# Changelog

## Unreleased

### Added
- UI layout tests to verify button alignment and form positioning (#22)

### Fixed
- Dashboard quick action buttons now use stable grid layout instead of random rotations and overlaps (#22)
- Transfer form submit button positioning corrected (no longer pushed off-screen) (#22)
- Kubernetes readiness probe path corrected from `/healthz` to `/health` (#22)

- Initial intentionally vulnerable demo banking application for AI SDLC demo.
