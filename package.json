{
  "name": "demobank-ai-sdlc",
  "version": "1.0.0",
  "description": "DemoBank AI SDLC - intentionally vulnerable demo banking app",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "seed": "node scripts/seed.js",
    "semgrep:demo": "semgrep scan --config .semgrep.yml .",
    "semgrep:owasp": "semgrep scan --config p/owasp-top-ten --config p/secrets ."
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
